<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="board">
	<select id="boardList" resultType="boardDTO">
		SELECT * FROM boardview LIMIT 0, 10
	</select>
	
	<select id="detail" parameterType="int" resultType="Map">
		SELECT board_no, board_title, board_content, 
		(SELECT mname FROM member WHERE mno = board.mno) AS mname, board_date, board_ip 
		FROM board 
		WHERE board_no=#{no} 
	</select>

	<select id="detail2" parameterType="String" resultType="boardDTO">
		SELECT board_no, board_title, board_content, 
		(SELECT mname FROM member WHERE mno = board.mno) AS mname, board_date, board_ip 
		FROM board 
		WHERE board_no=#{no} 
	</select>
</mapper>