<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="board">

	<resultMap type="commentDTO" id="commentDTOmap">
		<result column="cno" property="no"/>
		<result column="board_no" property="board_no"/>
		<result column="ccomment" property="comment"/>
		<result column="cdate" property="cdate"/>
		<result column="clike" property="clike"/>
		<result column="mid" property="mid"/>
		<result column="mname" property="mname"/>
		<result column="cip" property="cip"/>
		<result column="mno" property="mno"/>
	</resultMap>

	<select id="boardList" resultType="boardDTO">
		SELECT board_no, board_title, mname, board_date, board_count, comment 
		FROM boardview LIMIT 0, 10 
	</select>
	
	<select id="detail" parameterType="int" resultType="Map">
		SELECT b.board_no, b.board_title, b.board_content, m.mname, b.board_date, b.board_ip, b.board_del, 
		(SELECT COUNT(*) FROM comment WHERE b.board=#{board_no})comment 
		FROM board b JOIN member m ON b.mno=m.mno 
		WHERE board_no=#{no} 
	</select>

	<select id="detail2" parameterType="Integer" resultType="boardDTO">
		SELECT board_no, board_title, board_content,  
		(SELECT mname FROM member WHERE mno = board.mno) AS mname, board_date, board_ip, board_del, 
		(SELECT COUNT(*) FROM comment WHERE board_no=board.board_no) AS comment 
		FROM board 
		WHERE board_no=#{no} AND board_del='1' 
	</select>
	
	<insert id="write" parameterType="writeDTO">
		INSERT INTO board (board_title, board_content, mno) 
		VALUES (#{title}, #{content}, (SELECT mno FROM member WHERE mid=#{mid})) 
		<selectKey resultType="Integer" keyProperty="board_no" order="AFTER">
			SELECT LAST_INSERT_ID()
		</selectKey>
	</insert>
	
	<insert id="commentWrite" parameterType="commentDTO">
		INSERT INTO comment (board_no, ccomment, mno) 
		VALUES (#{no}, #{comment}, (SELECT mno FROM member WHERE mid=#{mid}))
	</insert>
	
	<select id="commentsList" parameterType="Integer" resultMap="commentDTOmap">
		SELECT cno, ccomment, clike, cdate, mid, mname, cip, mno, board_no 
		FROM commentview 
		WHERE board_no=#{no}
	</select>
</mapper>